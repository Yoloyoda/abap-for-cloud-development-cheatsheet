projection;
strict ( 1 );
use draft;

define behavior for ZC_BLOB_TEST alias BLOB_TEST
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}